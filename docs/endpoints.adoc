= Endpoint Reference
:toc:
:toclevels: 3
:sectanchors:

== Health

[cols="1,3,1"]
|===
|Method |Path |Auth

|GET
|`/health`
|No
|===

== Corpus And Sessions

[cols="1,3,1"]
|===
|Method |Path |Auth

|GET
|`/corpus/metadata`
|Yes

|POST
|`/search`
|Yes

|GET
|`/session/{sessionId}`
|Yes
|===

== Decision Endpoints

[cols="1,3,1"]
|===
|Method |Path |Auth

|GET
|`/decision/{identifier}`
|Yes

|GET
|`/decision/{identifier}/hits`
|Yes

|POST
|`/decision/{identifier}/semantic-search`
|Yes

|POST
|`/decision/{identifier}/fulltext-search`
|Yes
|===

== Search And Workspace Building

[cols="1,3,1"]
|===
|Method |Path |Auth

|POST
|`/decisions/search-within-dataframe`
|Yes

|POST
|`/decisions/filter`
|Yes

|POST
|`/arguments/expand`
|Yes

|POST
|`/hits/expand-to-arguments`
|Yes
|===

== Generic Dataframe Operations

[cols="1,3,1"]
|===
|Method |Path |Auth

|POST
|`/dataframe/describe`
|Yes
|===

== Typed Decision Dataframe Operations

[cols="1,3,1"]
|===
|Method |Path |Auth

|POST
|`/decisions/query`
|Yes

|POST
|`/decisions/filter`
|Yes

|POST
|`/decisions/aggregate`
|Yes
|===

== Typed Hit Dataframe Operations

[cols="1,3,1"]
|===
|Method |Path |Auth

|POST
|`/hits/query`
|Yes

|POST
|`/hits/filter`
|Yes

|POST
|`/hits/aggregate`
|Yes
|===

== Typed Argument Dataframe Operations

[cols="1,3,1"]
|===
|Method |Path |Auth

|POST
|`/arguments/query`
|Yes

|POST
|`/arguments/filter`
|Yes

|POST
|`/arguments/aggregate`
|Yes
|===

== Dataframe References

The API uses immutable dataframe references that typically look like:

----
nsoud-search-session:<sessionId>:decisions
nsoud-search-session:<sessionId>:hits
nsoud-search-session:<sessionId>:arguments
----

Expected kinds by endpoint family:

- `decisions/*` typed dataframe endpoints expect a `:decisions` ref
- `hits/*` typed dataframe endpoints expect a `:hits` ref
- `arguments/*` typed dataframe endpoints expect an `:arguments` ref
- generic `/dataframe/describe` accepts any dataframe ref

== Search Result Limits

Hybrid search endpoints expose two separate knobs:

- `limit` controls the maximum number of final ranked decision results retained in the search session and dataframe refs
- `topRowsLimit` controls how many preview rows are returned inline in the immediate response
- `candidate_count` is the count of final ranked results before inline preview truncation by `topRowsLimit`

This separation applies to:

- `POST /search`
- `POST /decisions/search-within-dataframe`

For scoped search, linked precedent expansion is allowed only when the linked decision is already inside the provided decision scope.

== Typed Request Contracts

The public contract is intentionally strict:

- decision, hit, and argument dataframe endpoints use endpoint-specific request DTOs
- field names, enum values, and allowed filters differ by endpoint family
- decision semantic and fulltext search take `identifier` from the path, not from the JSON body
- decision detail field projection uses repeated `select` query parameters such as `select=source.full_text`

Use the generated OpenAPI spec in `openapi/fragmea-api.openapi.yaml` as the source of truth for field-level request and response details.
